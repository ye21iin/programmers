WITH RECURSIVE TIMELINE AS (
    SELECT 0 AS HOUR
    UNION ALL
    SELECT HOUR+1
    FROM TIMELINE
    WHERE HOUR < 23
)

SELECT T.HOUR, COUNT(A.ANIMAL_ID) AS COUNT
FROM ANIMAL_OUTS A
RIGHT OUTER JOIN TIMELINE T ON T.HOUR = HOUR(A.DATETIME)
GROUP BY T.HOUR
ORDER BY T.HOUR
